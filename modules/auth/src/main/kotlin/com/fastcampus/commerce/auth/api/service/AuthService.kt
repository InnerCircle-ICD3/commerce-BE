package com.fastcampus.commerce.auth.api.service

import org.springframework.stereotype.Service

@Service
class AuthService {

    fun reissueToken(accessToken: String): String {
        // accessToken 검증

        // accessToken 반환
        return "d"
    }
}
